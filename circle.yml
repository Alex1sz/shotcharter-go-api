machine:
  environment:
    IMPORT_PATH: "home/ubuntu/.go_project/src/github.com/$CIRCLE_PROJECT_USERNAME"
    APP_PATH: "$IMPORT_PATH/$CIRCLE_PROJECT_REPONAME"
  services:
    - postgresql
database:
  override:
    - createdb shotcharter_go_test
    - psql shotcharter_go_test -f db/schema_setup.sql
dependencies:
  override:
    - sudo add-apt-repository ppa:masterminds/glide -y
    - sudo apt-get update
    - sudo apt-get install glide -y
test:
  pre:
    - mkdir -p "$IMPORT_PATH"
    - ln -sf "$(pwd)" "$APP_PATH"
    - cd "$APP_PATH" && glide install
  override:
    - cd "$APP_PATH" && go test -v $(glide novendor)
